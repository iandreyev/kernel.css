.ion-hero {
    background: linear-gradient(to right, #fafafa, #eeeeee);
    height: 500px;

    .ion-container {
        position: relative;
        text-align: center;
        transform: translateY(-50%);
        top: 50%;
    }

    .ion-hero-header {
        color: #424242;
        font-size: 4.5em;
        font-weight: normal;
        margin: 0;
     }

    .ion-hero-paragraph {
         color: #424242;
         font-size: 1.6em;
     }
}
